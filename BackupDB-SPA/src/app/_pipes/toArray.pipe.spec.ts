/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { toArrayPipe } from './toArray.pipe';

describe('Pipe: Derpe', () => {
  it('create an instance', () => {
    let pipe = new toArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
